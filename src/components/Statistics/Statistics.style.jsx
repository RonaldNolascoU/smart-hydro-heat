import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    bottomContainer: {
        marginTop: 20,
        marginBottom: 20,
        alignItems: 'center',
    },
    bottomButton: {
        width: '85%'
    },
    cardContainer: {
        width: '90%',
        borderRadius: 0,
        marginBottom: 15,
        marginTop: 10,
        marginRight: 'auto',
        marginLeft: 'auto'

    },
    cardMessages: {
        borderRadius: 0
    },
    cardImage: {
        borderRadius: 0
    }
})